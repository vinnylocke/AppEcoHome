# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
.env.tmp
.env.*.local
dist
dist-ssr
*.local

# Build-time version stamp (written by scripts/deploy.mjs before the
# Vercel build, consumed by useAppVersion at runtime).
public/build-version.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

supabase/snippets/
supabase/.env
# Supabase CLI cache + temp state (CLI version pin, etc.) — local only.
supabase/.temp/
.claude/

# Generated test output
playwright-report/
test-results/
.vercel

# Beta invite list — keep emails out of version control
beta-invites.txt
