// Garden Reports were moved to email delivery (monthly + yearly via the
// garden-reports edge function). The in-app Reports tab has been removed.
// E2E coverage for report delivery is handled by the edge function's own
// integration tests. This file is intentionally empty.
