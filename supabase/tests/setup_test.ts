import { assertEquals } from "@std/assert";

// Placeholder — confirms the Deno test runner and import map are wired up correctly.
// Delete this file once real tests exist in weather-rules/ or patterns/.
Deno.test("runner is configured", () => {
  assertEquals(1 + 1, 2);
});
