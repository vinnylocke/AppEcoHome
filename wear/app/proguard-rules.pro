# ProGuard/R8 rules for release builds. Empty for now — release is unminified
# in Phase 0 (see app/build.gradle.kts). Add keep rules here if minification is
# enabled later and reflection-based libs (e.g. serialization) need them.
